# API base URL — points to the backend
# In development: http://localhost:5000/api (proxied by Vite)
# In production with Docker: leave empty (nginx proxies /api to the server container)
VITE_API_URL=http://localhost:5000/api
