MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/approvalportal?retryWrites=true&w=majority
JWT_SECRET=change_this_to_a_long_random_string_in_production
PORT=5000
NODE_ENV=development
# Allowed CORS origin for the frontend (comma-separated for multiple)
CLIENT_ORIGIN=http://localhost:3000
