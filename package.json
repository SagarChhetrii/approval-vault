{
  "name": "approval-portal",
  "private": true,
  "scripts": {
    "setup": "npm --prefix server install && npm --prefix client install",
    "dev:clean-ports": "kill-port 3000 3001 4000 5000 || true",
    "dev": "npm run dev:clean-ports && concurrently -k -n SERVER,CLIENT -c green,cyan \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev"
  },
  "devDependencies": {
    "kill-port": "^2.0.1",
    "concurrently": "^9.2.1"
  }
}
